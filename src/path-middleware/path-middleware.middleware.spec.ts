import { PathMiddlewareMiddleware } from './path-middleware.middleware';

describe('PathMiddlewareMiddleware', () => {
  it('should be defined', () => {
    expect(new PathMiddlewareMiddleware()).toBeDefined();
  });
});
